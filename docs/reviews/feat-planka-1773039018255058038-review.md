# Review: feat/planka-1773039018255058038-prisma-schema-migrations

**ticket_id:** `planka-1773039018255058038`
**card:** http://localhost:3333/cards/1773039018255058038
**branch:** `feat/planka-1773039018255058038-prisma-schema-migrations`
**data do review:** 2026-05-12
**reviewer:** Claude (automatizado via /proj-review)

---

## Critérios de Aceite

| # | Critério | Status | Evidência no código |
|---|---|---|---|
| CA1 | Executar `pnpm db:migrate` gera migration em `prisma/migrations/<ts>_init/migration.sql` com todas as tabelas | **OK** | `apps/api/prisma/migrations/20260512042406_init/migration.sql` — 7 tabelas + 4 enums criados |
| CA2 | `pnpm db:seed` cria 1 usuário TEACHER, 1 quiz publicado, ≥2 questões com 4 opções cada | **OK** | `apps/api/prisma/seed.ts:26-104` — upsert de 1 professor, 1 quiz, 2 questões, 4 opções cada |
| CA3 | Prisma Studio exibe as 7 tabelas após migration | **OK** | Migration SQL cria: `User`, `Quiz`, `Question`, `AnswerOption`, `GameSession`, `GameParticipant`, `GameAnswer` |
| CA4 | `pnpm type-check` em `apps/api` sem erros | **OK** | Sem uso de `any`; tipos Prisma usados corretamente; importações ESM com extensão `.js` ✅ |

---

## Análise Qualitativa

### Aderência à spec

Implementação completa e fiel à spec aprovada. Todos os 5 índices de performance especificados foram adicionados:

| Modelo | Índice | Presente |
|---|---|---|
| `GameSession` | `@@index([status])` | ✅ `schema.prisma:74` |
| `GameParticipant` | `@@index([sessionId])` | ✅ `schema.prisma:88` |
| `GameAnswer` | `@@index([sessionId])` | ✅ `schema.prisma:106` |
| `GameAnswer` | `@@index([questionId])` | ✅ `schema.prisma:107` |
| `Question` | `@@index([quizId, order])` | ✅ `schema.prisma:48` |

O `package.json` foi atualizado com `"prisma": { "seed": "tsx prisma/seed.ts" }` e o script `"db:seed"` — exatamente conforme a spec (`apps/api/package.json:20-22`).

### Qualidade

**Seed (`seed.ts`):**
- Upsert usado em todos os registros → idempotência garantida ✅
- `SEED_IDS` exportado como `const` permite reuso seguro nos testes ✅
- Função `seed()` separada do `main()` → testabilidade ✅
- Guard ESM (`isMain`) evita execução acidental ao importar como módulo ✅ (`seed.ts:123-126`)
- Senha hasheada com `argon2` — adequado para dados de desenvolvimento ✅

**Testes (`seed.test.ts`):**
- Cobre os 6 cenários da spec + 1 bônus (`should return questions ordered by the order field`)
- Usa banco real (sem mocks do PrismaClient) — correto para testes de integração
- Cleanup cuidadoso: `afterAll` remove registros na ordem correta (dependentes primeiro)
- Teste de constraint única verifica código de erro Prisma `P2002` — correto
- Cleanup local no teste de nickname duplicado (`gameSession` / `gameParticipant`) ✅

**Migration SQL:**
- Todos os FKs com `ON DELETE RESTRICT` (comportamento conservador e seguro por padrão)
- Cascade somente onde a spec exige: `Question → AnswerOption` e `Quiz → Question` ✅
- Todos os unique constraints presentes (`User_email_key`, `GameSession_pin_key`, `GameParticipant_sessionId_nickname_key`, `GameAnswer_participantId_questionId_key`) ✅

### Sugestões (não bloqueantes)

1. **Cleanup parcial no teste de constraint:** Se o teste `'should reject with unique constraint violation (P2002)'` falhar antes de chegar na limpeza manual (`deleteMany` / `delete`), o `GameSession` de pin `'999001'` ficará órfão no banco de teste. Considere mover a criação/limpeza da sessão para `beforeEach`/`afterEach` ou usar uma transação revertida — mas não bloqueia aprovação.

2. **Senha do seed hardcoded:** `'Professor@2026'` está em texto plano no `seed.ts`. Para uso exclusivo de dev/teste, é aceitável; para ambientes compartilhados, considere ler de `process.env.SEED_TEACHER_PASSWORD` com fallback.

---

## Aderência a React Practices (aplicável ao backend)

| Convenção | Status | Evidência |
|---|---|---|
| TypeScript strict (sem `any`) | **OK** | Nenhum `any` encontrado em `seed.ts` ou `seed.test.ts` |
| Sem valores hardcoded (URLs, configurações) | **OK** | `DATABASE_URL` via env; IDs via `SEED_IDS` constante |
| Estrutura de arquivos | **OK** | `prisma/seed.ts`, `prisma/__tests__/seed.test.ts` — organização coerente |
| Imutabilidade | **N/A** | Código de infraestrutura de banco |

---

## Aderência a React Testing

> Nota: trata-se de testes de integração de backend (Vitest + PrismaClient real). Regras de UI são N/A.

| Convenção | Status | Evidência | Bloqueante |
|---|---|---|---|
| `data-testid` formato `type-action-component` | **N/A** | Backend — sem elementos de UI | -- |
| `userEvent` do test-utils | **N/A** | Backend — sem interação de usuário | -- |
| `findBy*` em vez de `waitFor + getBy*` | **N/A** | Backend — sem queries de DOM | -- |
| Factory functions tipadas | **OK** | `SEED_IDS` tipado como `const`; dados realistas em pt-BR | -- |
| Sem mock de fetch/axios direto | **OK** | PrismaClient real usado — correto para integração | -- |
| Testes em inglês | **OK** | Todos os `describe`/`it` em inglês; fixtures em pt-BR ✅ | -- |

---

## Veredicto

**Aprovado** ✅

```
Veredicto: Aprovado

Implementação completa e fiel à spec. Todos os 5 índices de performance adicionados,
migration inicial gerada corretamente com as 7 tabelas e todos os constraints.
Seed idempotente com upsert, seed function exportada e testável. Testes de integração
cobrem todos os 6 cenários da spec (+ 1 bônus), usando banco real conforme as regras
do projeto. Sem uso de `any`, sem valores hardcoded indevidos. Duas sugestões menores
não bloqueantes foram registradas acima.
```
