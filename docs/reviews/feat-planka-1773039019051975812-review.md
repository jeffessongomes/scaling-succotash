# Review — feat/planka-1773039019051975812-frontend-setup

**PR:** #10 — feat: Frontend — Setup Next.js 15 + shadcn/ui + scaffolding completo
**Branch:** `feat/planka-1773039019051975812-frontend-setup`
**Ticket:** `planka-1773039019051975812`
**Card Planka:** [Frontend: Setup Next.js 15 + Tailwind + shadcn/ui](http://localhost:3333/cards/1773039019051975812)
**Data:** 2026-05-12
**Revisor:** Claude (automatizado via `/proj-review`)

---

## Critérios de Aceite

| # | Critério | Status | Evidência no código |
|---|---|---|---|
| CA1 | `pnpm dev` sobe sem erros em localhost:3000 | OK | `pnpm type-check` zero erros; estrutura de app completa em `apps/web/src/app/` |
| CA2 | `pnpm dlx shadcn@latest add button` gera componente sem conflito | OK | `src/components/ui/button.tsx` já gerado pelo CLI; `components.json` configurado com `"aliases.ui": "@/components/ui"` |
| CA3 | `{ socket }` de `@/lib/socket` retorna instância Socket.io com `autoConnect: false` | OK | `src/lib/socket.ts:4` — `autoConnect: false`; coberto em `socket.test.ts` (3 testes) |
| CA4 | `pnpm type-check` retorna zero erros TypeScript | OK | Confirmado: 4/4 pacotes passam sem erros |
| CA5 | Todos os testes passam | OK | 15/15 testes passando em 4 arquivos (`ui.store.test.ts`, `socket.test.ts`, `useMediaQuery.test.ts`, `test-utils.test.tsx`) |
| CA6 | `pnpm lint` retorna zero erros ESLint | OK | Confirmado: `✔ No ESLint warnings or errors` |

---

## Análise Qualitativa

### Aderência à spec

**O que foi implementado conforme a spec:**
- `lib/socket.ts` com singleton e `autoConnect: false` — idêntico ao exemplo da spec
- `stores/ui.store.ts` com `sidebarOpen`, `setSidebarOpen`, `toggleSidebar` — idêntico ao exemplo da spec
- `constants/game.ts` com todos os 5 valores definidos — idêntico ao exemplo da spec
- `types/index.ts` com `GamePhase` e `UITheme` — idêntico ao exemplo da spec
- `hooks/useMediaQuery.ts` — idêntico ao exemplo da spec
- Estrutura de diretórios `features/` com `.gitkeep` — segue o diagrama de arquitetura
- Estrutura de diretórios `components/` com `ui/`, `game/`, `quiz/`, `shared/` — conforme spec
- `api-client.ts` com interceptor de resposta — presente

**Desvios aceitáveis:**

1. **`tailwind.config.ts` ausente:** A spec listava este arquivo como entregável, mas a implementação utilizou corretamente o paradigma do Tailwind v4 — configuração via `@theme inline` em `globals.css` ao invés de `tailwind.config.ts`. O `components.json` reflete isso com `"config": ""`. Os tokens do tema Azimute (primary roxo `#7C3AED`, secondary azul `#2563EB`, accent âmbar `#F59E0B`, success verde, danger vermelho) estão presentes no `globals.css` via CSS custom properties + dark mode. Esta é a abordagem idiomática para Tailwind v4.

2. **`components/ui/button.tsx` no lugar de `.gitkeep`:** A spec previa `ui/.gitkeep` como entregável inicial. O PR foi além e já inicializou o shadcn/ui com o componente `Button`. Isso não é uma violação — satisfaz o CA2 diretamente e é o resultado esperado do `pnpm dlx shadcn@latest init`.

3. **`src/lib/utils.ts` não previsto na spec:** Gerado automaticamente pelo CLI do shadcn/ui como utilitário `cn()`. Necessário para o `button.tsx` e componentes shadcn em geral. Aceitável.

**Gaps menores:**

- **`index.ts` barrels em `features/`:** A Regra 5 da spec menciona "apenas arquivos `index.ts` de barrel e diretórios vazios" nas features. O PR criou apenas `.gitkeep` sem os barrels. O diagrama de arquitetura da spec mostra apenas `.gitkeep`, o que é inconsistente com o texto da regra. Gap de baixa criticidade — os barrels serão criados naturalmente quando as features forem implementadas.

- **`api-client.ts` interceptor:** O interceptor adicionado é um esqueleto válido, mas ambos os branches da condição 401 fazem a mesma coisa (`Promise.reject(error)`). O comentário `// Auth errors are handled by NextAuth session logic` documenta a intenção, mas o interceptor não acrescenta comportamento diferenciado hoje. Funcional para esta fase de scaffolding.

### Qualidade

- **Testes unitários bem escritos:** Os 3 novos suites (`socket`, `ui.store`, `useMediaQuery`) cobrem os cenários definidos na spec com boa legibilidade.
- **`useMediaQuery.test.ts`:** Mock de `window.matchMedia` implementado como factory local (`mockMatchMedia`) com cleanup via `vi.restoreAllMocks()`. Cobre retorno inicial, atualização por evento e cleanup no unmount — satisfaz os cenários da spec.
- **`ui.store.test.ts`:** Reset de estado via `useUIStore.setState({ sidebarOpen: false })` no `beforeEach` — boa prática para evitar vazamento entre testes.
- **`socket.test.ts`:** `vi.resetModules()` no `beforeEach` garante que cada teste carregue o módulo limpo — correto para módulos singleton.

---

## Aderência a React Practices

| Convenção | Status | Evidência |
|---|---|---|
| TypeScript strict (sem `any`) | OK | Nenhum `any` encontrado; cast em `useMediaQuery.test.ts:22` usa `unknown` como intermediário — correto |
| `data-testid` em elementos interativos | N/A | PR é scaffolding; nenhum componente de UI com elementos interativos criado neste PR |
| Estrutura de arquivos (components/features/hooks) | OK | Todos os diretórios criados conforme `react-practices.md` |
| Imutabilidade (sem mutação) | OK | Zustand store usa `set()` imutável; `useMediaQuery` não muta estado diretamente |
| Sem hardcoded (env vars, constantes) | OK | `socket.ts` usa `process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000'`; `api-client.ts` usa `process.env['NEXT_PUBLIC_API_URL']` |

---

## Aderência a React Testing

| Convenção | Status | Evidência | Bloqueante |
|---|---|---|---|
| `data-testid` formato `type-action-component` | N/A | Nenhum componente UI com interação testada | -- |
| `userEvent` do test-utils | N/A | Nenhum teste de interação UI | -- |
| `findBy*` em vez de `waitFor + getBy*` | N/A | Nenhuma query assíncrona nos testes | -- |
| Factory functions tipadas | N/A | Sem dados complexos neste ticket | -- |
| Sem mock de fetch/axios direto | OK | `socket.test.ts:3` — mock de `socket.io-client` via `vi.mock()` (fronteira correta) | -- |
| Testes em inglês | OK | Todos os `describe`/`it` em inglês | -- |
| `renderHook` importado de test-utils | Violação | `useMediaQuery.test.ts:2` — importa de `@testing-library/react` diretamente; test-utils re-exporta via `export *`, então o comportamento é idêntico | Não (arquivo novo, mas violação é inócua) |

> **Nota sobre o `renderHook`:** A regra de usar test-utils é explícita para `render`, `screen` e `userEvent`. O `renderHook` não é mencionado explicitamente, mas como test-utils faz `export * from '@testing-library/react'`, o correto seria importar de `../test/test-utils`. A violação é inócua em runtime mas inconsistente com as convenções do projeto.

---

## Veredicto

```
Veredicto: Aprovado com comentários

O PR satisfaz todos os 6 critérios de aceite com evidências concretas (type-check zero erros,
15/15 testes passando, lint limpo). A ausência de tailwind.config.ts é um desvio intencional e
correto para Tailwind v4, com o tema Azimute implementado via globals.css. Os demais desvios
(utils.ts gerado pelo CLI, button.tsx instalado, .gitkeep sem index.ts barrels) são aceitáveis
para um ticket de scaffolding.

Sugestões não-bloqueantes:
1. Alinhar import do renderHook em useMediaQuery.test.ts para usar ../test/test-utils
   (em vez de @testing-library/react diretamente), seguindo a convenção do projeto.
2. Considerar remover o dead code do interceptor 401 em api-client.ts (ambos os branches
   fazem Promise.reject) ou adicionar comportamento diferenciado (ex: log, toast) quando
   a feature de auth for implementada.
3. Criar index.ts barrels nos diretórios features/auth/, features/quiz/ e features/game/
   para facilitar imports nas features futuras.
```
