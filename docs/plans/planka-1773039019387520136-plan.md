# Plano de Implementação: Tela de Host (Lobby + Controle da Partida)

**Ticket:** `planka-1773039019387520136`
**Branch:** `feat/planka-1773039019387520136-host-lobby`
**Spec:** `docs/specs/planka-1773039019387520136-spec.md`

---

## Notas de Infraestrutura

### Antes de começar
- [x] `pnpm add qrcode.react` (em apps/web)
- [x] `pnpm add -D @types/qrcode.react` (em apps/web)
- [x] Adicionar `NEXT_PUBLIC_APP_URL=http://localhost:3000` em `apps/web/.env.local`

### Após implementação
- [ ] Validar `NEXT_PUBLIC_APP_URL` em staging/produção

---

## Tasks

### Task 1 — Tipos do domínio game
**Arquivos:** `apps/web/src/features/game/types/index.ts`
**Testes:** nenhum (tipos puros)
**Ação:** Criar `GameSession`, `GamePlayer`, `QuestionState`, `AnswerStats`, `LeaderboardEntry`

---

### Task 2 — `useHostGame.ts` (TDD)
**Arquivos:**
- `apps/web/src/features/game/hooks/useHostGame.ts`
- `apps/web/src/features/game/hooks/__tests__/useHostGame.test.ts`

**RED → GREEN:**
- socket emit `host:create-session` no mount
- `session:created` → setar sessão + fase lobby
- `session:player-joined` → adicionar jogador
- `session:question-start` → fase question + currentQuestion
- `session:answer-received` → atualizar answeredCount
- `session:answers-revealed` → fase results + answerStats
- `session:leaderboard` → fase leaderboard + leaderboard
- `session:ended` → fase finished
- `session:error` → connectionError

**Verificar:** `pnpm test apps/web/src/features/game/hooks/__tests__/useHostGame.test.ts`

---

### Task 3 — `QRCodeDisplay.tsx`
**Arquivos:** `apps/web/src/components/shared/QRCodeDisplay.tsx`
**Testes:** nenhum (wrapper puro de lib — testado via WaitingLobby)

---

### Task 4 — `WaitingLobby.tsx` (TDD)
**Arquivos:**
- `apps/web/src/components/game/WaitingLobby.tsx`
- `apps/web/src/components/game/__tests__/WaitingLobby.test.tsx`

**RED → GREEN:**
- Renderiza PIN
- Renderiza QR Code (`data-testid="img-qrcode-session"`)
- Lista vazia → "Aguardando jogadores…"
- Botão "Iniciar" desabilitado com 0 jogadores
- Botão "Iniciar" habilitado com ≥ 1 jogador
- Jogador aparece na lista

**Verificar:** `pnpm test apps/web/src/components/game/__tests__/WaitingLobby.test.tsx`

---

### Task 5 — `QuestionControl.tsx`
**Arquivos:** `apps/web/src/components/game/QuestionControl.tsx`
**Testes:** cobertos pelo orquestrador (page.tsx)

---

### Task 6 — `AnswerReveal.tsx`
**Arquivos:** `apps/web/src/components/game/AnswerReveal.tsx`
**Testes:** cobertos pelo orquestrador (page.tsx)

---

### Task 7 — `LeaderboardView.tsx`
**Arquivos:** `apps/web/src/components/game/LeaderboardView.tsx`
**Testes:** cobertos pelo orquestrador (page.tsx)

---

### Task 8 — `FinishedView.tsx`
**Arquivos:** `apps/web/src/components/game/FinishedView.tsx`
**Testes:** cobertos pelo orquestrador (page.tsx)

---

### Task 9 — `host/page.tsx` (TDD — orquestrador)
**Arquivos:**
- `apps/web/src/app/(dashboard)/quizzes/[id]/host/page.tsx`
- `apps/web/src/app/(dashboard)/quizzes/[id]/host/page.test.tsx`

**RED → GREEN (transições de fase):**
- Lobby inicial renderiza WaitingLobby
- Iniciar → QuestionControl
- Revelar → AnswerReveal
- Ver Placar → LeaderboardView
- Próxima Pergunta → QuestionControl
- Encerrar → ConfirmDialog → FinishedView
- Banner "Reconectando…" visível quando isConnecting

**Verificar:** `pnpm test apps/web/src/app`

---

### Task 10 — Exports do feature/game
**Arquivos:** `apps/web/src/features/game/index.ts`
**Testes:** nenhum

---

### Task 11 — Validação final
```bash
pnpm test
pnpm lint
pnpm type-check
pnpm build
```
