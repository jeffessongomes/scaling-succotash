# Plano: Backend — CRUD de Perguntas e Opções de Resposta

**Ticket ID:** `planka-1773039018699654270`  
**Branch:** `feat/planka-1773039018699654270-crud-perguntas-opcoes`  
**Spec:** `docs/specs/planka-1773039018699654270-spec.md`

---

## Tasks

### Task 0 — Foundation

**Arquivos:** `apps/api/src/shared/errors/http-errors.ts`  
**Alteração:** Adicionar `UnprocessableEntityError` (422)  
**Verificação:** `pnpm type-check`

---

### Task 1 — Question: Types + Schemas

**Arquivos a criar:**
- `apps/api/src/features/question/question.types.ts`
- `apps/api/src/features/question/question.schemas.ts`

**Conteúdo:**
- Interface `QuestionPublic`
- Schemas Zod: `CreateQuestionBodySchema`, `UpdateQuestionBodySchema`, `ReorderQuestionBodySchema`
- Validação cruzada `mediaType`/`mediaUrl` no `refine`

**Verificação:** `pnpm type-check`

---

### Task 2 — Question: Testes (RED)

**Arquivo a criar:** `apps/api/src/features/question/__tests__/question.router.test.ts`

**Cenários (18):**
- POST 201 body válido
- POST 401 sem auth
- POST 403 quiz de outro professor
- POST 404 quiz inexistente
- POST 201 timeLimitSecs=5 (boundary min)
- POST 400 timeLimitSecs=4
- POST 201 timeLimitSecs=120 (boundary max)
- POST 400 timeLimitSecs=121
- POST 400 mediaUrl sem mediaType
- PATCH 200 campos parciais válidos
- PATCH 400 body vazio
- PATCH 404 pergunta inexistente
- PATCH 403 pergunta de outro professor
- DELETE 204 pergunta do próprio quiz
- DELETE 403 pergunta de outro professor
- DELETE 404 pergunta inexistente
- PATCH /reorder 200 order válido
- PATCH /reorder 401 sem auth

**Verificação:** `pnpm test apps/api/src/features/question` → deve FALHAR (RED)

---

### Task 3 — Question: Service + Router (GREEN)

**Arquivos a criar:**
- `apps/api/src/features/question/question.service.ts`
- `apps/api/src/features/question/question.router.ts`

**Service — funções públicas:**
- `createQuestion(quizId, body, user)` — `assertQuizOwnership` + `getNextOrder`
- `updateQuestion(id, body, user)` — busca `question → quiz`, verifica ownership
- `deleteQuestion(id, user)` — verifica ownership, deleta (cascade no schema)
- `reorderQuestion(id, order, user)` — verifica ownership, atualiza só `order`

**Router — rotas:**
```
POST   /quizzes/:quizId/questions
PATCH  /questions/:id
DELETE /questions/:id
PATCH  /questions/:id/reorder
```

**Verificação:** `pnpm test apps/api/src/features/question` → deve PASSAR (GREEN)

---

### Task 4 — Registrar questionRouter em app.ts

**Arquivo:** `apps/api/src/app.ts`  
**Alteração:** Importar e registrar `questionRouter` em `/api`

**Verificação:** `pnpm test && pnpm type-check`

---

### Task 5 — Option: Types + Schemas

**Arquivos a criar:**
- `apps/api/src/features/option/option.types.ts`
- `apps/api/src/features/option/option.schemas.ts`

**Conteúdo:**
- Interface `AnswerOptionPublic`
- Schemas Zod: `CreateOptionBodySchema`, `UpdateOptionBodySchema`

**Verificação:** `pnpm type-check`

---

### Task 6 — Option: Testes (RED)

**Arquivo a criar:** `apps/api/src/features/option/__tests__/option.router.test.ts`

**Cenários (13):**
- POST 201 com < 4 opções existentes
- POST 422 com 4 opções (5ª)
- POST 201 isCorrect=true reseta as outras
- POST 401 sem auth
- POST 403 pergunta de outro professor
- POST 400 color inválida
- POST 404 pergunta inexistente
- PATCH 200 isCorrect=true reseta as outras
- PATCH 400 body vazio
- PATCH 404 opção inexistente
- PATCH 403 opção de outro professor
- DELETE 204 opção do próprio quiz
- DELETE 403 opção de outro professor
- DELETE 404 opção inexistente

**Verificação:** `pnpm test apps/api/src/features/option` → deve FALHAR (RED)

---

### Task 7 — Option: Service + Router (GREEN)

**Arquivos a criar:**
- `apps/api/src/features/option/option.service.ts`
- `apps/api/src/features/option/option.router.ts`

**Service — funções públicas:**
- `createOption(questionId, body, user)` — verifica max 4, `prisma.$transaction` se isCorrect=true
- `updateOption(id, body, user)` — verifica ownership, `prisma.$transaction` se isCorrect=true
- `deleteOption(id, user)` — verifica ownership via `option → question → quiz`

**Router — rotas:**
```
POST   /questions/:questionId/options
PATCH  /options/:id
DELETE /options/:id
```

**Verificação:** `pnpm test apps/api/src/features/option` → deve PASSAR (GREEN)

---

### Task 8 — Registrar optionRouter em app.ts

**Arquivo:** `apps/api/src/app.ts`  
**Alteração:** Importar e registrar `optionRouter` em `/api`

**Verificação:** `pnpm test && pnpm type-check`

---

### Task 9 — Validação Final

```bash
pnpm test
pnpm lint
pnpm type-check
pnpm build
```

---

### Task 10 — PR + Planka

- Abrir PR com template da spec
- Comentar no card Planka com link do PR
- Mover card para "Em Revisão / Teste"
