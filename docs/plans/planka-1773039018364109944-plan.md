# Plano de Implementação — Backend: Autenticação de Professores

**Ticket:** `planka-1773039018364109944`
**Branch:** `feat/planka-1773039018364109944-auth-professores`
**Criado em:** 2026-05-12

---

## Notas de Infraestrutura

### Antes de começar
- [x] `argon2` instalado no API
- [x] `next-auth@^5.0.0-beta.29` instalado no web
- [ ] Adicionar `AUTH_SECRET`, `AUTH_TRUST_HOST=true`, `NEXT_PUBLIC_API_URL=http://localhost:4000` em `apps/web/.env.local`

### Após implementação
- [ ] Validar variáveis de ambiente em staging/produção

---

## Task 1 — [RED] Testes de Integração do Router de Auth

**Arquivos:** `apps/api/src/features/auth/__tests__/auth.router.test.ts`

**Testes a escrever (8 cenários):**
1. `POST /auth/register` → 201 (primeiro usuário)
2. `POST /auth/register` → 409 (já existe usuário)
3. `POST /auth/register` → 400 (email malformado)
4. `POST /auth/register` → 400 (senha < 8 chars)
5. `POST /auth/login` → 200 (credenciais válidas)
6. `POST /auth/login` → 401 (email não encontrado)
7. `POST /auth/login` → 401 (senha incorreta)
8. `POST /auth/login` → 400 (body inválido)

**Mocks:** `prisma.user.count`, `prisma.user.create`, `prisma.user.findUnique`, `argon2.hash`, `argon2.verify`

**Verificação:** `pnpm --filter api test src/features/auth/__tests__/auth.router.test.ts` → deve falhar (RED)

---

## Task 2 — [GREEN] Implementação da camada de Auth no API

**Arquivos:**
- `apps/api/src/features/auth/auth.schema.ts` — schemas Zod (LoginBody, RegisterBody)
- `apps/api/src/features/auth/auth.service.ts` — `createFirstAdmin()` + `validateCredentials()`
- `apps/api/src/features/auth/auth.router.ts` — `POST /auth/register`, `POST /auth/login`
- `apps/api/src/app.ts` — registrar `app.use('/auth', authRouter)`

**Verificação:** `pnpm --filter api test src/features/auth/__tests__/auth.router.test.ts` → deve passar (GREEN)

---

## Task 3 — [GREEN] Implementação Web (NextAuth + Middleware)

**Arquivos:**
- `apps/web/src/lib/auth.ts` — Credentials provider + callbacks jwt()/session() + pages
- `apps/web/src/middleware.ts` — proteção de /dashboard/* e /quizzes/*
- `apps/web/src/types/next-auth.d.ts` — extensão de tipos Session, User, JWT

**Verificação:** `pnpm --filter web type-check` → sem erros

---

## Task 4 — Validação Final

1. `pnpm test` — todos os testes passando
2. `pnpm lint` — 0 erros
3. `pnpm type-check` — 0 erros
4. `pnpm build` — build completo sem erros

---

## Task 5 — PR + Planka

1. `git push -u origin feat/planka-1773039018364109944-auth-professores`
2. Abrir PR via `gh pr create`
3. Comentar no card Planka e mover para "Em Revisão / Teste"
