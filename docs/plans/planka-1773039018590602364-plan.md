# Plano de Implementação: Backend — CRUD de Quizzes

**Ticket:** `planka-1773039018590602364`
**Branch:** `feat/planka-1773039018590602364-backend-crud-quizzes`
**Spec:** `docs/specs/planka-1773039018590602364-spec.md`

---

## Tasks

### Task 1 — Schemas e Tipos (`quiz.schemas.ts`)

**Arquivos:** `apps/api/src/features/quiz/quiz.schemas.ts`

- `CreateQuizBodySchema`: title (min 1, max 200), description?, coverImage? (url)
- `UpdateQuizBodySchema`: campos opcionais + refine corpo não vazio
- `PublishQuizBodySchema`: isPublished boolean
- Tipos `QuizSummary` e `QuizDetail` exportados
- **Verificação:** `pnpm type-check`

---

### Task 2 — Testes (RED) (`quiz.router.test.ts`)

**Arquivo:** `apps/api/src/features/quiz/__tests__/quiz.router.test.ts`

Suites:
- POST /api/quizzes (CA-01, CA-13, CA-14)
- GET /api/quizzes (CA-02, CA-03, CA-14)
- GET /api/quizzes/:id (CA-04, CA-05, CA-14)
- PATCH /api/quizzes/:id (CA-06, CA-07, edge cases)
- DELETE /api/quizzes/:id (CA-11, CA-12)
- PATCH /api/quizzes/:id/publish (CA-08, CA-09, CA-10)

- **Verificação:** `pnpm test apps/api/src/features/quiz` → deve FALHAR (RED)

---

### Task 3 — Service (GREEN) (`quiz.service.ts`)

**Arquivo:** `apps/api/src/features/quiz/quiz.service.ts`

Funções:
- `assertQuizOwnership(quizId, user)` — 404 se não existe, 403 se não autorizado
- `createQuiz(body, user)` → QuizSummary
- `listQuizzes(user)` → QuizSummary[] (TEACHER: próprios; ADMIN: todos)
- `getQuizDetail(id, user)` → QuizDetail (perguntas e opções ordenadas)
- `updateQuiz(id, body, user)` → QuizSummary
- `deleteQuiz(id, user)` → void (transação: finaliza sessões ativas → deleta quiz)
- `publishQuiz(id, body, user)` → QuizSummary (valida ≥1 pergunta com ≥2 opções ao publicar)

---

### Task 4 — Router (GREEN) (`quiz.router.ts`) + registro em `app.ts`

**Arquivos:**
- `apps/api/src/features/quiz/quiz.router.ts`
- `apps/api/src/app.ts`

Rotas:
```
POST   /api/quizzes               → 201 QuizSummary
GET    /api/quizzes               → 200 QuizSummary[]
GET    /api/quizzes/:id           → 200 QuizDetail
PATCH  /api/quizzes/:id           → 200 QuizSummary
DELETE /api/quizzes/:id           → 204
PATCH  /api/quizzes/:id/publish   → 200 QuizSummary
```

---

### Task 5 — Validação Final

```bash
pnpm test        # todos os testes devem passar
pnpm lint        # 0 erros
pnpm type-check  # 0 erros
pnpm build       # 0 erros
```

---

### Task 6 — PR + Planka

- Abrir PR com template da spec
- Comentar no card Planka
- Mover card para "Em Revisão / Teste"
