# Plano: Backend — Engine do Jogo (WebSocket + Redis)

**ticket_id:** `planka-1773039018817094784`
**Branch:** `feat/planka-1773039018817094784-backend-engine-jogo`
**Spec:** `docs/specs/planka-1773039018817094784-spec.md`

---

## Dependências Externas

- [ ] Instalar `socket.io-client` + `@types/socket.io-client` como devDependencies (necessário para `game.socket.test.ts`)

---

## Task 1 — Tipos + State Machine

**Arquivos:**
- `apps/api/src/features/game/game.types.ts`
- `apps/api/src/features/game/game.state-machine.ts`
- `apps/api/src/features/game/__tests__/game.state-machine.test.ts`

**RED:** Escrever `game.state-machine.test.ts` verificando transições válidas, inválidas e `InvalidTransitionError`.
**GREEN:** Implementar `VALID_TRANSITIONS`, `canTransition`, `assertTransition`, `InvalidTransitionError`.
**VERIFY:** `pnpm test src/features/game/__tests__/game.state-machine.test.ts`

---

## Task 2 — Pontuação

**Arquivos:**
- `apps/api/src/shared/utils/scoring.ts`
- `apps/api/src/shared/utils/__tests__/scoring.test.ts`

**RED:** Testar resposta em 0ms (100%), no limite (50%), além do limite (0%), em 50% (75%), points_base=0.
**GREEN:** Implementar `calculateScore(pointsBase, timeLimitMs, answeredInMs)`.
**VERIFY:** `pnpm test src/shared/utils/__tests__/scoring.test.ts`

---

## Task 3 — Gerador de PIN

**Arquivos:**
- `apps/api/src/shared/utils/pin-generator.ts`
- `apps/api/src/shared/utils/__tests__/pin-generator.test.ts`

**RED:** Testar PIN com 6 dígitos, sem zeros à esquerda, colisão no Redis.
**GREEN:** Implementar `generateUniquePin(redis)` com até 10 tentativas.
**VERIFY:** `pnpm test src/shared/utils/__tests__/pin-generator.test.ts`

---

## Task 4 — Schemas Zod

**Arquivos:**
- `apps/api/src/features/game/game.schemas.ts`

**Sem testes unitários** — coberto pelos testes de router/socket.

---

## Task 5 — Cache Redis

**Arquivos:**
- `apps/api/src/infrastructure/cache/game-state.cache.ts`
- `apps/api/src/infrastructure/cache/__tests__/game-state.cache.test.ts`

**RED:** Testar saveSession, getSession, deleteSession, saveAnswer (HSETNX), updateScore (ZADD).
**GREEN:** Implementar operações Redis com TTL de `env.GAME_SESSION_TTL`.
**VERIFY:** `pnpm test src/infrastructure/cache/__tests__/game-state.cache.test.ts`

---

## Task 6 — Service

**Arquivos:**
- `apps/api/src/features/game/game.service.ts`
- `apps/api/src/features/game/__tests__/game.service.test.ts`

**RED:** Testar createSession (PIN gerado, saved no DB+Redis), getSessionByPin (Redis first), finalizeSession (persiste GameAnswer).
**GREEN:** Implementar usando Prisma + game-state.cache.
**VERIFY:** `pnpm test src/features/game/__tests__/game.service.test.ts`

---

## Task 7 — Router REST

**Arquivos:**
- `apps/api/src/features/game/game.router.ts`
- `apps/api/src/features/game/__tests__/game.router.test.ts`

**RED:** Testar POST/GET/DELETE de sessões com supertest.
**GREEN:** Implementar rotas com autenticação e validação Zod.
**VERIFY:** `pnpm test src/features/game/__tests__/game.router.test.ts`

---

## Task 8 — Socket.io Handlers

**Arquivos:**
- `apps/api/src/features/game/game.socket.ts`
- `apps/api/src/features/game/__tests__/game.socket.test.ts`

**RED:** Testar ciclo completo com socket.io-client (join → start → answer → reveal → leaderboard → end).
**GREEN:** Implementar `setupGameSocket(io)` com state machine + timers.
**VERIFY:** `pnpm test src/features/game/__tests__/game.socket.test.ts`

---

## Task 9 — Wiring

**Arquivos modificados:**
- `apps/api/src/server.ts` → exportar `io`, chamar `setupGameSocket(io)`
- `apps/api/src/app.ts` → registrar `gameRouter` em `/api`

**VERIFY:** `pnpm lint && pnpm type-check`

---

## Validação Final

```bash
pnpm test
pnpm lint
pnpm type-check
pnpm build
```
