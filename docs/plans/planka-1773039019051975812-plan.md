# Plano de Implementação — Frontend: Setup Next.js 15 + Tailwind + shadcn/ui

**Ticket:** `planka-1773039019051975812`
**Branch:** `feat/planka-1773039019051975812-frontend-setup`
**Data:** 2026-05-12

---

## Notas de Infraestrutura

### Antes de começar
- [x] Branch `feat/planka-1773039019051975812-frontend-setup` criada
- [ ] `pnpm dlx shadcn@latest init` executado em `apps/web/`
- [ ] `.env.local` com `NEXT_PUBLIC_API_URL=http://localhost:4000`

### Após implementação
- [ ] Verificar que `pnpm dev` sobe sem erros
- [ ] Verificar que `pnpm dlx shadcn@latest add button` gera componente sem conflito

---

## Tasks

### Task 1 — shadcn/ui init + tailwind.config.ts
- **Arquivos:** `components.json` (novo), `tailwind.config.ts` (novo), `src/app/globals.css` (modificar)
- **RED:** Verificar que `components.json` não existe
- **GREEN:** Executar shadcn init não-interativo + criar `tailwind.config.ts` com paleta Azimute
- **VERIFY:** `pnpm type-check` zero erros

### Task 2 — lib/socket.ts + teste
- **Arquivos:** `src/lib/socket.ts` (novo), `src/lib/socket.test.ts` (novo)
- **RED:** Escrever `socket.test.ts` → falha (arquivo não existe)
- **GREEN:** Criar `src/lib/socket.ts` com singleton
- **VERIFY:** `pnpm test apps/web/src/lib/socket.test.ts`

### Task 3 — ui.store.ts + teste
- **Arquivos:** `src/stores/ui.store.ts` (novo), `src/stores/ui.store.test.ts` (novo)
- **RED:** Escrever teste → falha (store não existe)
- **GREEN:** Criar store Zustand
- **VERIFY:** `pnpm test apps/web/src/stores/ui.store.test.ts`

### Task 4 — useMediaQuery.ts + teste
- **Arquivos:** `src/hooks/useMediaQuery.ts` (novo), `src/hooks/useMediaQuery.test.ts` (novo)
- **RED:** Escrever teste → falha (hook não existe)
- **GREEN:** Criar hook
- **VERIFY:** `pnpm test apps/web/src/hooks/useMediaQuery.test.ts`

### Task 5 — types + constants
- **Arquivos:** `src/types/index.ts` (novo), `src/constants/game.ts` (novo)
- **GREEN:** Criar arquivos (sem testes — configuração estática)
- **VERIFY:** `pnpm type-check`

### Task 6 — Scaffold de diretórios
- **Arquivos:** Diretórios `components/`, `features/` com `.gitkeep`
- **GREEN:** Criar estrutura completa de pastas
- **VERIFY:** `ls` confirma estrutura

### Task 7 — api-client.ts interceptor
- **Arquivos:** `src/lib/api-client.ts` (modificar se necessário)
- **GREEN:** Adicionar interceptor de resposta para erros padronizados
- **VERIFY:** `pnpm type-check`

### Task 8 — Validação final
- `pnpm test` (suite completa)
- `pnpm lint`
- `pnpm type-check`
- `pnpm build`
