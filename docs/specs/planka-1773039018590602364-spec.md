# Spec: Backend — CRUD de Quizzes

**Ticket:** `planka-1773039018590602364`
**Card Planka:** [Backend: CRUD de Quizzes](http://localhost:3333/cards/1773039018590602364)
**Branch:** `feat/planka-1773039018590602364-backend-crud-quizzes`
**Spec gerada em:** 2026-05-12

---

## Objetivo

Implementar o módulo REST de CRUD de Quizzes no backend (`apps/api`), permitindo que professores autenticados criem, editem, publiquem e removam seus quizzes. É a base para o restante do sistema (perguntas já existem; sessões de jogo dependem de quizzes publicados).

---

## Modelo de Dados

O modelo `Quiz` **já existe** no Prisma schema (`apps/api/prisma/schema.prisma`). Nenhuma migration nova é necessária para os campos do quiz em si. Porém, é necessário uma migration para adicionar `onDelete: Cascade` em `GameSession → Quiz` (ver Regras de Negócio #5).

### Modelo Prisma atual

```prisma
model Quiz {
  id          String        @id @default(cuid())
  title       String
  description String?
  coverImage  String?
  isPublished Boolean       @default(false)
  author      User          @relation(fields: [authorId], references: [id])
  authorId    String
  questions   Question[]    // onDelete: Cascade já configurado em Question
  sessions    GameSession[] // sem cascade — tratar no serviço
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt
}
```

### Tipos TypeScript (a criar em `quiz.schemas.ts`)

```typescript
// Resposta de listagem (sem perguntas)
type QuizSummary = {
  id: string
  title: string
  description: string | null
  coverImage: string | null
  isPublished: boolean
  authorId: string
  _count: { questions: number }
  createdAt: Date
  updatedAt: Date
}

// Resposta de detalhe (com perguntas e opções)
type QuizDetail = QuizSummary & {
  questions: Array<{
    id: string
    quizId: string
    text: string
    mediaType: MediaType | null
    mediaUrl: string | null
    timeLimitSecs: number
    points: number
    order: number
    options: Array<{
      id: string
      questionId: string
      text: string
      isCorrect: boolean
      color: OptionColor
      order: number
    }>
  }>
}
```

---

## Regras de Negócio

1. **Criação:** Qualquer usuário autenticado (TEACHER ou ADMIN) pode criar um quiz. O `authorId` é sempre o `req.user.id`.

2. **Listagem:**
   - TEACHER: retorna apenas os quizzes com `authorId === req.user.id`.
   - ADMIN: retorna todos os quizzes do sistema.
   - Ordenação: `createdAt DESC` (mais recentes primeiro).

3. **Leitura de detalhe:** Qualquer usuário autenticado pode ler os detalhes de um quiz. As perguntas são retornadas ordenadas por `order ASC`; as opções também por `order ASC`.

4. **Edição de metadados (PATCH /api/quizzes/:id):** Somente o autor ou ADMIN pode editar. Campos editáveis: `title`, `description`, `coverImage`. O campo `isPublished` **não** é editável por esta rota — usar `/publish`.

5. **Remoção (DELETE /api/quizzes/:id):** Somente o autor ou ADMIN pode remover.
   - Se houver `GameSession` com `status` diferente de `FINISHED`, o serviço deve marcar essas sessões como `FINISHED` (status update) antes de excluir o quiz.
   - O Prisma cascade (`Question → AnswerOption`) já cuida das perguntas e opções automaticamente.
   - `GameSession` não tem cascade no schema atual — o serviço precisa deletar ou finalizar as sessões antes do delete do quiz. **Decisão:** finalizar (`status = FINISHED`) as sessões ativas e em seguida deletar o quiz em uma **transação Prisma**.

6. **Publicação (PATCH /api/quizzes/:id/publish):** Somente o autor ou ADMIN pode publicar/despublicar.
   - Para **publicar** (`isPublished: true`): o quiz precisa ter ao menos **1 pergunta** com ao menos **2 opções de resposta**.
   - Para **despublicar** (`isPublished: false`): sempre permitido, sem validações adicionais.
   - Rota aceita body `{ isPublished: boolean }` para controle explícito (não toggle implícito — mais previsível para o cliente).

7. **Sem soft delete:** O modelo `Quiz` não possui `deletedAt`. O delete é permanente.

---

## Critérios de Aceite

**CA-01 — Criar quiz**
> **Dado** um professor autenticado,
> **Quando** faz POST /api/quizzes com `{ title: "Quiz de História", description: "..." }`,
> **Então** retorna 201 com o quiz criado e `authorId` do usuário logado; `isPublished` é `false`.

**CA-02 — Listar quizzes**
> **Dado** um professor autenticado,
> **Quando** faz GET /api/quizzes,
> **Então** retorna 200 com array contendo apenas os quizzes deste professor, ordenados por `createdAt DESC`, com `_count.questions`.

**CA-03 — Listar todos (admin)**
> **Dado** um ADMIN autenticado,
> **Quando** faz GET /api/quizzes,
> **Então** retorna 200 com todos os quizzes do sistema.

**CA-04 — Detalhe do quiz**
> **Dado** um usuário autenticado,
> **Quando** faz GET /api/quizzes/:id com um ID válido,
> **Então** retorna 200 com o quiz, suas perguntas (order ASC) e opções de cada pergunta (order ASC).

**CA-05 — Detalhe inexistente**
> **Dado** um usuário autenticado,
> **Quando** faz GET /api/quizzes/:id com ID inexistente,
> **Então** retorna 404.

**CA-06 — Editar metadados**
> **Dado** o autor do quiz autenticado,
> **Quando** faz PATCH /api/quizzes/:id com `{ title: "Novo Título" }`,
> **Então** retorna 200 com os dados atualizados.

**CA-07 — Editar quiz de outro professor**
> **Dado** um professor autenticado que NÃO é o autor,
> **Quando** faz PATCH /api/quizzes/:id,
> **Então** retorna 403.

**CA-08 — Publicar quiz válido**
> **Dado** o autor com quiz que tem ao menos 1 pergunta com ao menos 2 opções,
> **Quando** faz PATCH /api/quizzes/:id/publish com `{ isPublished: true }`,
> **Então** retorna 200 com `isPublished: true`.

**CA-09 — Publicar quiz inválido**
> **Dado** o autor com quiz sem perguntas (ou com perguntas sem 2 opções),
> **Quando** faz PATCH /api/quizzes/:id/publish com `{ isPublished: true }`,
> **Então** retorna 422 com mensagem explicativa.

**CA-10 — Despublicar**
> **Dado** o autor com quiz publicado,
> **Quando** faz PATCH /api/quizzes/:id/publish com `{ isPublished: false }`,
> **Então** retorna 200 com `isPublished: false` (sem validação de perguntas).

**CA-11 — Deletar quiz sem sessões ativas**
> **Dado** o autor do quiz,
> **Quando** faz DELETE /api/quizzes/:id e não há GameSession ativa,
> **Então** retorna 204; quiz e perguntas são removidos.

**CA-12 — Deletar quiz com sessões ativas**
> **Dado** o autor do quiz com GameSession em status `ACTIVE`,
> **Quando** faz DELETE /api/quizzes/:id,
> **Então** as sessões são marcadas como `FINISHED` e o quiz é removido; retorna 204.

**CA-13 — Campos obrigatórios**
> **Dado** um professor autenticado,
> **Quando** faz POST /api/quizzes sem o campo `title`,
> **Então** retorna 400.

**CA-14 — Sem autenticação**
> **Dado** requisição sem token,
> **Quando** acessa qualquer rota do módulo,
> **Então** retorna 401.

---

## Edge Cases

1. **Publicar quiz com perguntas mas todas sem opções:** retorna 422.
2. **Publicar quiz com perguntas e apenas 1 opção por pergunta:** retorna 422 (exige mínimo 2 opções).
3. **Deletar quiz já deletado:** retorna 404 (Prisma lança exceção de record not found).
4. **PATCH com body vazio `{}`:** retorna 400 (`Ao menos um campo deve ser fornecido`).
5. **ADMIN edita/deleta quiz de qualquer professor:** permitido (sem verificação de ownership para ADMIN).
6. **Listagem com zero quizzes:** retorna 200 com array vazio `[]`.
7. **`coverImage` com URL inválida:** retorna 400 (validação Zod).
8. **`title` com mais de 200 caracteres:** retorna 400.
9. **DELETE de quiz publicado com sessão FINISHED:** permitido (apenas sessões ativas são problema).
10. **GET /api/quizzes/:id de quiz de outro professor (não admin):** retorna 200 — leitura é pública para autenticados.

---

## UI

Este ticket é exclusivamente **backend**. Não há UI envolvida.

---

## Arquitetura

### Arquivos a criar

```
apps/api/src/features/quiz/
  ├── quiz.router.ts         # Rotas Express
  ├── quiz.service.ts        # Lógica de negócio + Prisma
  ├── quiz.schemas.ts        # Zod schemas + tipos exportados
  └── __tests__/
      └── quiz.router.test.ts
```

### Arquivos a modificar

| Arquivo | Mudança |
|---|---|
| `apps/api/src/app.ts` | Importar e registrar `quizRouter` em `app.use('/api', quizRouter)` |

### Rotas

```
POST   /api/quizzes                  → criar quiz
GET    /api/quizzes                  → listar quizzes (professor: próprios; admin: todos)
GET    /api/quizzes/:id              → detalhe com perguntas e opções
PATCH  /api/quizzes/:id              → editar metadados (title, description, coverImage)
DELETE /api/quizzes/:id              → remover
PATCH  /api/quizzes/:id/publish      → publicar/despublicar
```

### Middleware por rota

Todas as rotas requerem `requireAuth`. Nenhuma requer `requireAdmin` (ownership é verificado no service).

### Funções do service (`quiz.service.ts`)

```typescript
assertQuizOwnership(quizId: string, user: AuthenticatedUser): Promise<void>
createQuiz(body: CreateQuizBody, user: AuthenticatedUser): Promise<QuizSummary>
listQuizzes(user: AuthenticatedUser): Promise<QuizSummary[]>
getQuizDetail(id: string, user: AuthenticatedUser): Promise<QuizDetail>
updateQuiz(id: string, body: UpdateQuizBody, user: AuthenticatedUser): Promise<QuizSummary>
deleteQuiz(id: string, user: AuthenticatedUser): Promise<void>
publishQuiz(id: string, body: PublishQuizBody, user: AuthenticatedUser): Promise<QuizSummary>
```

### Zod Schemas (`quiz.schemas.ts`)

```typescript
CreateQuizBodySchema = z.object({
  title: z.string().min(1).max(200),
  description: z.string().max(1000).optional(),
  coverImage: z.string().url().optional(),
})

UpdateQuizBodySchema = z.object({
  title: z.string().min(1).max(200).optional(),
  description: z.string().max(1000).nullable().optional(),
  coverImage: z.string().url().nullable().optional(),
}).refine(d => Object.keys(d).length > 0, { message: 'Ao menos um campo deve ser fornecido' })

PublishQuizBodySchema = z.object({
  isPublished: z.boolean(),
})
```

### Constantes de select Prisma

```typescript
const QUIZ_SUMMARY_SELECT = {
  id: true, title: true, description: true, coverImage: true,
  isPublished: true, authorId: true, createdAt: true, updatedAt: true,
  _count: { select: { questions: true } },
}

const QUIZ_DETAIL_SELECT = {
  ...QUIZ_SUMMARY_SELECT,
  questions: {
    select: { id, quizId, text, mediaType, mediaUrl, timeLimitSecs, points, order,
      options: { select: { id, questionId, text, isCorrect, color, order }, orderBy: { order: 'asc' } }
    },
    orderBy: { order: 'asc' },
  },
}
```

### Transação de delete com sessões ativas

```typescript
// Em deleteQuiz():
await prisma.$transaction([
  prisma.gameSession.updateMany({
    where: { quizId: id, status: { not: 'FINISHED' } },
    data: { status: 'FINISHED' },
  }),
  prisma.quiz.delete({ where: { id } }),
])
```

### Lógica de publicação

```typescript
// Em publishQuiz() quando isPublished === true:
const quiz = await prisma.quiz.findUnique({
  where: { id },
  include: { questions: { include: { _count: { select: { options: true } } } } },
})
const isEligible = quiz.questions.some(q => q._count.options >= 2)
if (!isEligible) throw new UnprocessableEntityError(
  'O quiz precisa ter ao menos 1 pergunta com no mínimo 2 opções para ser publicado'
)
```

---

## Dependências de Infraestrutura

Nenhuma dependência de infraestrutura identificada. O módulo usa apenas Prisma (já configurado), Express (já configurado) e Zod (já instalado).

---

## Estratégia de Testes

### Framework e localização
- **Vitest** + **supertest** (para teste de integração de rotas HTTP)
- Arquivo: `apps/api/src/features/quiz/__tests__/quiz.router.test.ts`

### Cenários obrigatórios

#### Suite: POST /api/quizzes
- [ ] Cria quiz com dados válidos → 201 + quiz no body
- [ ] Rejeita sem autenticação → 401
- [ ] Rejeita sem `title` → 400
- [ ] `authorId` é o usuário autenticado (não aceita outro authorId no body)

#### Suite: GET /api/quizzes
- [ ] Retorna apenas quizzes do professor autenticado
- [ ] ADMIN recebe todos os quizzes
- [ ] Retorna array vazio quando professor não tem quizzes → 200 `[]`
- [ ] Rejeita sem autenticação → 401

#### Suite: GET /api/quizzes/:id
- [ ] Retorna quiz com perguntas e opções aninhadas
- [ ] Retorna 404 para ID inexistente
- [ ] Rejeita sem autenticação → 401

#### Suite: PATCH /api/quizzes/:id
- [ ] Edita `title` com sucesso → 200
- [ ] Rejeita edição por não-autor → 403
- [ ] ADMIN consegue editar quiz de qualquer professor → 200
- [ ] Body vazio → 400
- [ ] ID inexistente → 404

#### Suite: DELETE /api/quizzes/:id
- [ ] Deleta quiz sem sessões ativas → 204
- [ ] Deleta quiz com sessão ACTIVE → finaliza sessão + 204
- [ ] Rejeita delete por não-autor → 403
- [ ] ID inexistente → 404

#### Suite: PATCH /api/quizzes/:id/publish
- [ ] Publica quiz com 1+ pergunta com 2+ opções → 200 `isPublished: true`
- [ ] Rejeita publicação de quiz sem perguntas → 422
- [ ] Rejeita publicação de quiz com pergunta sem 2 opções → 422
- [ ] Despublica quiz publicado → 200 `isPublished: false` (sem validar perguntas)
- [ ] Rejeita por não-autor → 403

### Padrão de mocks

Usar `vi.mock` para mockar `../../config/database.js` (PrismaClient) — seguir padrão dos módulos existentes.

---

## Fora de Escopo

- Nenhum campo novo no schema Prisma (além da decisão sobre `GameSession` — tratada em serviço)
- Upload de `coverImage` — rota de upload de imagens é um ticket separado; `coverImage` aceita URL externa por ora
- Paginação na listagem — MVP não exige
- Busca/filtro de quizzes — ticket separado
- Frontend CRUD de quizzes — ticket `1773039014874448958`
- Engine de jogo / GameSession CRUD — ticket separado
