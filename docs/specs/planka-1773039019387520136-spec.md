# Spec: Frontend — Tela de Host (Lobby + Controle da Partida)

**Ticket:** `planka-1773039019387520136`
**Card Planka:** http://localhost:3333/cards/1773039019387520136
**Branch:** `feat/planka-1773039019387520136-host-lobby`

---

## Objetivo

Implementar a tela do professor host em `/quizzes/[id]/host`, responsável por criar uma sessão de jogo e controlá-la do início ao fim. É a tela central do MVP: sem ela, o jogo não começa.

A tela cobre dois momentos distintos:
1. **Lobby** — aguardando alunos se conectarem via PIN/QR Code
2. **Controle ativo** — avançando perguntas, revelando respostas, exibindo placar e encerrando

---

## Modelo de Dados

### Tipos TypeScript (novos em `features/game/types/index.ts`)

```ts
export interface GameSession {
  pin: string
  quizId: string
  quizTitle: string
  totalQuestions: number
  status: GamePhase
}

export interface GamePlayer {
  nickname: string
  avatarId: string
}

export interface QuestionState {
  index: number       // 0-based
  total: number
  text: string
  timeLimitSecs: number
  points: number
}

export interface AnswerStats {
  correctOptionId: string
  stats: Record<string, number>  // optionId → count
}

export interface LeaderboardEntry {
  nickname: string
  avatarId: string
  score: number
  rank: number
}
```

`GamePhase` já existe em `src/types/index.ts`:
```ts
export type GamePhase = 'lobby' | 'question' | 'results' | 'leaderboard' | 'finished'
```

### Constantes de jogo (já em `constants/game.ts`)

Reutilizar `GAME_CONSTANTS.MAX_PLAYERS`, `GAME_CONSTANTS.QUESTION_DURATION_SECONDS`.

---

## Regras de Negócio

1. **Criação de sessão** — ao acessar `/quizzes/[id]/host`, o cliente emite `host:create-session` via Socket.io; o servidor responde com o PIN gerado (6 dígitos).
2. **Acesso restrito** — a rota requer sessão Auth.js ativa (professor autenticado). O middleware já cobre `/quizzes/**`.
3. **Lobby** — a lista de jogadores cresce em tempo real conforme `session:player-joined` é recebido. Máximo de `MAX_PLAYERS` (45).
4. **Iniciar partida** — só habilitado quando há ao menos 1 jogador no lobby.
5. **Fluxo de pergunta** — após `game:start`, o servidor emite `session:question-start`. O host vê o enunciado e aguarda respostas. O contador "X de Y responderam" atualiza via `session:answer-received`.
6. **Revelar respostas** — host emite `game:reveal-answers`; servidor responde com `session:answers-revealed` contendo a opção correta e estatísticas por opção.
7. **Ver Placar** — host emite `game:show-leaderboard`; servidor responde com `session:leaderboard`.
8. **Próxima Pergunta** — emite `game:next-question`; disponível após revelar respostas ou ver placar.
9. **Encerrar** — emite `game:end`; disponível em qualquer fase pós-lobby. Confirmar antes de encerrar.
10. **Reconexão** — se o socket cair, tentar reconectar automaticamente (Socket.io built-in); o servidor restaura o estado da sessão via Redis.
11. **Quiz sem perguntas** — se `quiz.questions.length === 0`, bloquear acesso à tela de host com mensagem e link de volta ao editor.

---

## Critérios de Aceite

### Lobby

**Dado** que o professor acessa `/quizzes/[id]/host` com quiz publicado e com perguntas  
**Quando** a página carrega  
**Então** uma sessão é criada, o PIN de 6 dígitos é exibido em destaque, o QR Code aponta para `<APP_URL>/game/join?pin=<PIN>` e a lista de jogadores está vazia.

**Dado** que um aluno entra no lobby com o PIN  
**Quando** o evento `session:player-joined` chega  
**Então** o nickname + avatar aparecem na lista em tempo real sem recarregar a página.

**Dado** que nenhum aluno entrou  
**Quando** o professor clica em "Iniciar"  
**Então** o botão está desabilitado e um tooltip explica o motivo.

**Dado** que ao menos 1 aluno está no lobby  
**Quando** o professor clica em "Iniciar"  
**Então** o jogo começa e a fase muda para `question`.

### Controle da Partida

**Dado** que a partida está na fase `question`  
**Quando** o servidor emite `session:question-start`  
**Então** o host vê: índice da pergunta (ex: "Pergunta 2 de 7"), texto, tempo limite e o contador "0 de X responderam".

**Dado** que alunos vão respondendo  
**Quando** `session:answer-received` chega  
**Então** o contador atualiza para "N de X responderam".

**Dado** que o professor clica em "Revelar Respostas"  
**Quando** `session:answers-revealed` chega  
**Então** a opção correta é destacada e as estatísticas por opção são exibidas (porcentagem ou contagem).

**Dado** que o professor clica em "Ver Placar"  
**Quando** `session:leaderboard` chega  
**Então** o top 3 é exibido com nickname, avatar e pontuação.

**Dado** que a última pergunta foi revelada  
**Quando** o professor clica em "Próxima Pergunta"  
**Então** o sistema entende que é a última e habilita somente "Encerrar".

**Dado** que o professor clica em "Encerrar"  
**Quando** confirma o diálogo  
**Então** `game:end` é emitido e a fase muda para `finished` com o placar final exibido.

### Edge Cases (seção 6 expandida)

---

## Edge Cases

1. **Quiz sem perguntas** — renderizar banner de erro com link para o editor; não criar sessão.
2. **Quiz não publicado** — idem; o back-end rejeitará `host:create-session`, exibir mensagem de erro.
3. **Falha na criação de sessão** (socket erro) — exibir estado de erro com botão "Tentar novamente".
4. **Lobby cheio** (≥ 45 jogadores) — exibir aviso visual; novos `player:join` são rejeitados pelo servidor.
5. **Perda de conexão Socket.io** — exibir banner "Reconectando…"; suprimir controles; restaurar ao reconectar.
6. **Professor fecha a aba durante a partida** — ao retornar, o socket reconecta e o servidor emite o estado atual da sessão (Redis).
7. **Clicar "Encerrar" por engano** — diálogo de confirmação obrigatório antes de emitir `game:end`.
8. **Sessão já existente para o quiz** — o servidor deve retornar erro se houver sessão ativa; exibir mensagem "Já existe uma partida ativa para este quiz".
9. **`session:question-start` sem dados completos** — exibir estado de erro inline sem quebrar a página.
10. **Timeout de resposta na criação de sessão** (>10s sem resposta do socket) — exibir erro com retry.

---

## UI — Estados Visuais e Comportamentos

### Layout geral

Página full-screen (sem sidebar do dashboard). Navbar mínima com título do quiz e botão "Encerrar".

### Fase: `lobby`

```
┌──────────────────────────────────────┐
│  [← Cancelar]     Azimute Quiz       │
├──────────────────────────────────────┤
│                                      │
│  PIN DA SESSÃO                       │
│  ┌──────────────┐                   │
│  │   482 915    │   [QR Code]        │
│  └──────────────┘                   │
│  azimute.local/game/join             │
│                                      │
│  Jogadores (3/45)                    │
│  ┌─────┐ ┌─────┐ ┌─────┐           │
│  │ 🐶  │ │ 🦊  │ │ 🐱  │           │
│  │Ana  │ │João │ │Maria│           │
│  └─────┘ └─────┘ └─────┘           │
│                                      │
│  [Iniciar Partida ▶]  (desabilitado se 0 jogadores)  │
└──────────────────────────────────────┘
```

### Fase: `question`

```
┌──────────────────────────────────────┐
│  Pergunta 2 de 7          [Encerrar] │
├──────────────────────────────────────┤
│                                      │
│  "Qual é a capital do Brasil?"       │
│  ⏱ 30s   💎 1000pts                 │
│                                      │
│  Responderam: 12 de 25               │
│  ████████░░░░ (barra de progresso)   │
│                                      │
│       [Revelar Respostas]            │
└──────────────────────────────────────┘
```

### Fase: `results` (após revelar)

```
┌──────────────────────────────────────┐
│  Pergunta 2 de 7          [Encerrar] │
├──────────────────────────────────────┤
│                                      │
│  ✅ Brasília         → 20 (80%)      │
│  ❌ São Paulo        → 3  (12%)      │
│  ❌ Rio de Janeiro   → 2  (8%)       │
│  ❌ Belo Horizonte   → 0  (0%)       │
│                                      │
│  [Ver Placar]  [Próxima Pergunta ▶]  │
│  (botão direito desabilitado se última pergunta) │
└──────────────────────────────────────┘
```

### Fase: `leaderboard`

```
┌──────────────────────────────────────┐
│  Placar Parcial            [Encerrar]│
├──────────────────────────────────────┤
│  🥇 Ana        5.200 pts            │
│  🥈 João       4.800 pts            │
│  🥉 Maria      4.100 pts            │
│                                      │
│  [Próxima Pergunta ▶]                │
│  (ou [Encerrar] se última)          │
└──────────────────────────────────────┘
```

### Fase: `finished`

```
┌──────────────────────────────────────┐
│  Partida Encerrada                   │
├──────────────────────────────────────┤
│  Placar Final                        │
│  🥇 Ana        12.300 pts           │
│  🥈 João       11.700 pts           │
│  🥉 Maria      10.500 pts           │
│  ...                                 │
│                                      │
│  [← Voltar ao Dashboard]            │
└──────────────────────────────────────┘
```

### Estado de erro / reconexão

- Banner topo: "Conexão perdida — Reconectando…" em amarelo
- Botões de controle desabilitados durante reconexão

---

## Arquitetura

### Arquivos a criar

| Arquivo | Tipo | Responsabilidade |
|---|---|---|
| `apps/web/src/app/(dashboard)/quizzes/[id]/host/page.tsx` | Page | Orquestrador da UI, consome `useHostGame` |
| `apps/web/src/features/game/hooks/useHostGame.ts` | Hook | Estado do jogo + Socket.io events |
| `apps/web/src/features/game/types/index.ts` | Types | `GameSession`, `GamePlayer`, `QuestionState`, `AnswerStats`, `LeaderboardEntry` |
| `apps/web/src/features/game/api/game.api.ts` | API | `createSession(quizId, token)` via REST (se necessário para inicializar) |
| `apps/web/src/components/game/WaitingLobby.tsx` | Component | Exibe PIN, QR Code, lista de jogadores |
| `apps/web/src/components/game/QuestionControl.tsx` | Component | Controles durante fase `question` |
| `apps/web/src/components/game/AnswerReveal.tsx` | Component | Resultados da pergunta com estatísticas |
| `apps/web/src/components/game/LeaderboardView.tsx` | Component | Top 3 + posição |
| `apps/web/src/components/game/FinishedView.tsx` | Component | Placar final |
| `apps/web/src/components/shared/QRCodeDisplay.tsx` | Component | Wrapper de `qrcode.react` |

### Arquivos a modificar

| Arquivo | Mudança |
|---|---|
| `apps/web/src/constants/routes.ts` | Já tem `QUIZ_HOST` — nenhuma mudança necessária |
| `apps/web/src/features/game/index.ts` | Re-exportar hook e tipos |
| `apps/web/src/features/quiz/types/index.ts` | Nenhuma mudança; `QuizDetail` já cobre o necessário |

### `useHostGame.ts` — interface pública

```ts
interface UseHostGameReturn {
  // Estado da sessão
  session: GameSession | null
  phase: GamePhase
  isConnecting: boolean
  connectionError: string | null

  // Lobby
  players: GamePlayer[]

  // Pergunta ativa
  currentQuestion: QuestionState | null
  answeredCount: number

  // Resultados
  answerStats: AnswerStats | null

  // Placar
  leaderboard: LeaderboardEntry[]

  // Ações do host
  startGame: () => void
  nextQuestion: () => void
  revealAnswers: () => void
  showLeaderboard: () => void
  endGame: () => void
}
```

### Eventos Socket.io utilizados

**Emitidos pelo host:**

| Evento | Payload | Momento |
|---|---|---|
| `host:create-session` | `{ quizId }` | Mount da página |
| `game:start` | `{ pin }` | Botão "Iniciar" |
| `game:next-question` | `{ pin }` | Botão "Próxima Pergunta" |
| `game:reveal-answers` | `{ pin }` | Botão "Revelar Respostas" |
| `game:show-leaderboard` | `{ pin }` | Botão "Ver Placar" |
| `game:end` | `{ pin }` | Botão "Encerrar" (após confirmação) |

**Recebidos pelo host:**

| Evento | Payload | Ação |
|---|---|---|
| `session:created` | `{ pin, quizTitle, totalQuestions }` | Setar sessão, fase `lobby` |
| `session:player-joined` | `{ nickname, avatarId, totalPlayers }` | Adicionar jogador à lista |
| `session:question-start` | `{ question, questionIndex, totalQuestions }` | Fase `question` |
| `session:answer-received` | `{ totalAnswered, totalPlayers }` | Atualizar contador |
| `session:answers-revealed` | `{ correctOptionId, stats }` | Fase `results` |
| `session:leaderboard` | `{ top3 }` | Fase `leaderboard` |
| `session:ended` | `{ finalLeaderboard }` | Fase `finished` |
| `session:error` | `{ message }` | Exibir erro |

### Fluxo de fases

```
isConnecting
    ↓
lobby  →  [game:start]  →  question  →  [game:reveal-answers]  →  results
                               ↑                                       ↓
                        [game:next-question]            [game:show-leaderboard]
                               ↑                                       ↓
                            question  ←─────────────────  leaderboard
                                                                       ↓
                                                                  (última)
                                                                       ↓
                                                              [game:end] → finished
```

---

## Dependências de Infraestrutura

| Dependência | Tipo | Ação |
|---|---|---|
| `qrcode.react` | npm package | `pnpm add qrcode.react` + `pnpm add -D @types/qrcode.react` |
| `NEXT_PUBLIC_APP_URL` | Variável de ambiente | Adicionar em `.env.local` (usada para gerar URL no QR Code) |

---

## Estratégia de Testes

### Testes unitários — `useHostGame.ts`

Testar o hook isoladamente com socket mockado:

- `when socket emits session:created` → `session` populado, `phase === 'lobby'`
- `when socket emits session:player-joined` → `players` cresce
- `when socket emits session:question-start` → `phase === 'question'`, `currentQuestion` populado
- `when socket emits session:answer-received` → `answeredCount` atualiza
- `when socket emits session:answers-revealed` → `phase === 'results'`, `answerStats` populado
- `when socket emits session:leaderboard` → `phase === 'leaderboard'`, `leaderboard` populado
- `when socket emits session:ended` → `phase === 'finished'`
- `when socket emits session:error` → `connectionError` populado

### Testes de integração — `WaitingLobby.tsx`

- Renderiza PIN em destaque
- Renderiza QR Code (`data-testid="img-qrcode-session"`)
- Lista vazia exibe "Aguardando jogadores…"
- Botão "Iniciar" desabilitado com 0 jogadores
- Botão "Iniciar" habilitado com ≥ 1 jogador
- Jogador adicionado aparece na lista

### Testes de integração — `host/page.tsx` (orquestrador)

Cenários de transição de fase (todos com socket mockado):

- `lobby → question`: clicar "Iniciar" emite `game:start`; ao receber `session:question-start` a UI mostra `QuestionControl`
- `question → results`: clicar "Revelar" emite `game:reveal-answers`; ao receber `session:answers-revealed` a UI mostra `AnswerReveal`
- `results → leaderboard`: clicar "Ver Placar" emite `game:show-leaderboard`; ao receber `session:leaderboard` a UI mostra `LeaderboardView`
- `leaderboard → question`: clicar "Próxima Pergunta" emite `game:next-question`
- Clicar "Encerrar" abre `ConfirmDialog`; confirmar emite `game:end`; ao receber `session:ended` mostra `FinishedView`
- Reconexão: banner "Reconectando…" visível durante `isConnecting`, botões desabilitados

### Dados de teste (factory functions em pt-BR)

```ts
const createGameSession = (overrides?: Partial<GameSession>): GameSession => ({
  pin: '482915',
  quizId: 'quiz-abc-123',
  quizTitle: 'Capitais do Mundo',
  totalQuestions: 7,
  status: 'lobby',
  ...overrides,
})

const createGamePlayer = (overrides?: Partial<GamePlayer>): GamePlayer => ({
  nickname: 'Ana Silva',
  avatarId: 'avatar-cat',
  ...overrides,
})
```

Seguir todas as convenções de [.claude/rules/react-testing.md](.claude/rules/react-testing.md).

---

## Fora de Escopo

- Tela do aluno (`/game/play/[pin]`) — ticket separado
- Tela de display/projeção (`/game/display/[pin]`) — ticket separado
- Mídia nas perguntas (imagem/vídeo) durante o controle — Fase 2
- Animações de entrada/saída de jogadores no lobby
- Backend (Express + Socket.io handlers) — ticket separado (API)
- Persistência de histórico de sessões
- Modo de recuperação de sessão após F5 completo (fora do MVP)
